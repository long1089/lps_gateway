@{
    ViewData["Title"] = "访问被拒绝";
}

<div class="container mt-5">
    <div class="alert alert-danger">
        <h4 class="alert-heading">访问被拒绝</h4>
        <p>您没有权限访问此资源。</p>
        <hr>
        <p class="mb-0">
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary">返回首页</a>
        </p>
    </div>
</div>
